---
name: finish-phase
description: Decide whether a phase is finished — run the gates, open every named test, then launch the real build and walk the "Done when" paragraph.
---

The instructions for this command live in `.claude/commands/finish-phase.md`.

Read that file and follow it exactly. It is the single source of truth; this file exists only so the
command is discoverable from here.

Two copies of a command diverge within a month. One copy and a pointer does not.
