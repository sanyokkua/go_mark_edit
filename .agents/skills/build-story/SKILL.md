---
name: build-story
description: Implement one story — refuse a stub, capture a trustworthy baseline before any edit, then build, then verify every Definition-of-Done item against it.
---

The instructions for this command live in `.agents/commands/build-story.md`.

Read that file and follow it exactly. It is the single source of truth; this file exists only so the
command is discoverable from here.

Two copies of a command diverge within a month. One copy and a pointer does not.
