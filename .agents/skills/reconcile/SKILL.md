---
name: reconcile
description: Close the loop after a phase — diff what shipped against the specification and resolve every difference in one of three named directions.
---

The instructions for this command live in `.claude/commands/reconcile.md`.

Read that file and follow it exactly. It is the single source of truth; this file exists only so the
command is discoverable from here.

Two copies of a command diverge within a month. One copy and a pointer does not.
