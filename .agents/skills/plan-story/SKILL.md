---
name: plan-story
description: Expand one story stub in place — feature rules copied in whole, architecture rules injected by path-glob match, and a rendered Definition of Done naming a test per rule.
---

The instructions for this command live in `.agents/commands/plan-story.md`.

Read that file and follow it exactly. It is the single source of truth; this file exists only so the
command is discoverable from here.

Two copies of a command diverge within a month. One copy and a pointer does not.
