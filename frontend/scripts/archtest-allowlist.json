{
  "_why": "Violations that already existed when these gates were written on 2026-07-28. The gate is green with these and fails on anything new. Each is described in docs/delivery/plan/KNOWN_ISSUES.md. Lower a number when you fix one; never raise one.",
  "strings": {},
  "colours": {},
  "_parityRoutes_why": "T173 swept the substitutions out of production; four files still carry a branch and each is documented at its site. CodeEditor holds Monaco capture conditions a measurement proved load-bearing for T059's popup residual (181 -> 239 pixels when removed). EditorView holds a preview refresh state that never crosses the bridge, so no mock seed can intercept it and a static reference cannot express an in-flight refresh. EditorChrome's substituted overflow menu is BLOCKED, not kept: removing it moved the 375px settings-overflow capture by 38px (bounds.left 140.375 vs 102.375) because the shipped icon-first overflow is narrower than the mockup's flat text list - a real structural difference needing a reference variant or an owner decision, tracked on T173. AppShell's two are the data-parity-shell and data-parity-family attributes that 66 CSS rules across six stylesheets key off. Lower a number when a branch leaves; never raise one.",
  "parityRoutes": {
    "src/ui/components/CodeEditor.tsx": 1,
    "src/ui/widgets/AppShell.tsx": 2,
    "src/ui/widgets/EditorChrome.tsx": 1,
    "src/ui/widgets/EditorView.tsx": 1
  }
}
