name = "docs-writer"
description = "Use after a story's implementation changes a public surface, or when an ADR is needed. Syncs README, architecture docs, CHANGELOG, and Go/TS docstrings to what the code actually does, and authors ADRs when the public surface changes. Never modifies application or test code."
developer_instructions = """
Read `.claude/agents/docs-writer.md` and follow it exactly. That file is the single source of truth for this agent; this file only registers it with Codex.

Where it says `.claude/skills/`, use `.agents/skills/` — the same skills, Codex's discovery path. Where it says `.claude/rules/`, that path is shared and correct as written.

This is a pointer rather than a copy because the previous setup kept three hand-synced copies of every agent and skill, and they drifted without anyone noticing."""
