# Editor-stage focused evidence

Captured 2026-08-04; the current T061-T062 gate summary is in `editor-stage-verification.md`.

## T061-T062 focused proof

| Evidence                                                                         | Result                                                                                                                                        |
| -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T061 registry/dispatcher/UI suites                                               | 9 suites passed, 42 tests passed                                                                                                              |
| `just verify 002-editor-stage-formatting`                                        | M1-M6 passed sequentially against the trustworthy baseline; M3 produced real parseable analysis                                               |
| `npm run verify:ui -- e2e/editor-stage.test.ts --reporter=line` from `frontend/` | 54/54 passed in one worker                                                                                                                    |
| `just build`                                                                     | Passed for fresh `darwin/arm64` packaged app                                                                                                  |
| `just fmt-check`                                                                 | Passed                                                                                                                                        |
| `npm --prefix frontend run typecheck`                                            | Passed                                                                                                                                        |
| `just lint`                                                                      | Passed; golangci reported 0 issues and ESLint reported 0 errors                                                                               |
| `just archtest`                                                                  | Passed: Go architecture, CGO-free build, migration immutability, frontend boundaries, colour tokens, and offline checks                       |
| `just frontend-build`                                                            | Passed; production network guard reported `ok`                                                                                                |
| `just gen-check`                                                                 | Exit 1 tracking result for required generated EditorSettings/UpdateEditor output and generator-owned modes; no generated file was hand-edited |
| `git diff --check`                                                               | Exit 2 only for generated `models.ts:346` and `:350` trailing whitespace                                                                      |

The generated Wails output was regenerated by the real `just build` and `just gen-check`. The repository's
tracking check reports the required uncommitted generated DTO/handler diff while these generated files are
part of the implementation; no generated file was hand-edited. The generated tracking state is separate
from the fresh M1-M6 verification result.

## T063 bounded-formatting proof

- RED: the new large-document regression failed at the existing `lineBounds` whole-document
  `source.split` call, proving the test detects the intended gap.
- GREEN: `frontend/src/logic/format/formatting.ts` now finds line boundaries with newline indexes,
  derives positions without copying a source prefix, and splits only the selected line window.
- `node_modules/.bin/jest --runInBand src/logic/format/formatting.test.ts src/logic/hooks/useDocumentCommands.test.ts src/ui/components/CodeEditor.test.tsx src/ui/widgets/EditorView.integration.test.tsx` from `frontend/`: 4 suites and 49 tests passed, including the T063 large-document case with a 50,000-line unrelated suffix and exact heading edit range.
- Full frontend regression: 53 suites and 239 tests passed. `just fmt-check`, `npm --prefix frontend run typecheck`, reliable escalated `just lint`, `just archtest`, and `just verify 002-editor-stage-formatting` all passed; the official verifier reported M1-M6 PASS.
- T063 changes only the bounded formatter helpers and their regression test. File/tab/Assistant/tidy/network
  deferrals and the native/generated-binding boundaries are unchanged.

## Behavioral proof

- `frontend/src/logic/format/formatting.test.ts` proves marker toggles, ATX heading replacement/removal,
  canonical `1. ` numbering without auto-renumbering, quote/link/table transformations, and bounded edits.
- `frontend/src/logic/actions/actionDispatcher.test.ts` proves deferred Format/Compact/Lint and shared
  deferred `toggle-assistant` return unavailable before any command invocation.
- `frontend/src/ui/widgets/EditorContextMenu.test.tsx` proves the exact surface-specific context inventory:
  Cut, Copy, Paste, Paste as plain text, Bold, Italic, Link, Format document, Compact, Command palette;
  Heading/list/Quote/Table and Lint are absent, and pointer activation keeps the menu action mounted.
- `frontend/src/ui/widgets/EditorChrome.test.tsx` proves toolbar pointer-down preserves the editor selection
  before a formatting action reads it.
- `frontend/src/ui/widgets/ShellMenuRow.test.tsx`, `EditorChrome.test.tsx`, `ShortcutsDialog.test.tsx`,
  and `App.test.tsx` prove the four top-level actions, visual-only tabs, toolbar/overflow, settings,
  arrangement, unavailable controls, and modal discovery surfaces.

## Retained convergence T043-T047

The final convergence proof on 2026-08-04 included the named focused suites:

| Evidence                                                                                                                                                                                  | Result                                                   |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| Action registry, dispatcher, shortcut/shell routing, formatter, document-command, CodeEditor, EditorView, EditorChrome, EditorContextMenu, ShortcutsDialog, and AppearanceControls suites | 13 suites passed, 80 tests passed                        |
| `just test`                                                                                                                                                                               | 50 suites passed, 208 tests passed; Go race tests passed |
| `npx playwright test e2e/editor-stage.test.ts` from `frontend/`                                                                                                                           | 27/27 passed across 1280/768/375 and all six palettes    |
| `just verify 002-editor-stage-formatting`                                                                                                                                                 | M1-M6 passed against the retained baseline               |

T043 now routes available toolbar/overflow/context/shell formatting and deferred keyboard identities
through the typed dispatcher, with focus/scope and no-invocation regression proof. T044 threads the
acknowledged bullet/emphasis markers through both visible formatting surfaces into the shared formatter;
T045 keeps Markdown standard a disabled menu item with no renderer persistence path; T046 applies headings
line by line within one bounded edit; and T047 derives context-menu accelerator metadata and exact
surface-specific ordering from the registry.

`just check` was also rerun. It stops at `gen-check` because the already-dirty Wails-generated DTO/handler
files and generator-owned mode bits differ from HEAD; no generated file was hand-edited or reverted. The
independent `just verify` gate passed all M1-M6.
