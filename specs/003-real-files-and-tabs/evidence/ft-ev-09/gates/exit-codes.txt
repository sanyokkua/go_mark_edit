# Feature 003-real-files-and-tabs release gate stack
commit: 9863f0d8
host:   Darwin arm64, go go1.26.6, node v24.18.0
start:  2026-08-16T22:05:47Z

just gen-check                         exit=0
just archtest                          exit=0
just check                             exit=0
go test -race ./internal/... .         exit=0
npm test --runInBand                   exit=0
go test -race -tags native_evidence ./cmd/... exit=0
just e2e-test                          exit=0
just verify 003-real-files-and-tabs    exit=0
just build                             exit=0

end:    2026-08-16T22:12:45Z

Notes
-----
Generated by `just release-stack` (scripts/release-stack.sh). The exit codes above are
mechanical; everything below this line is owed by a person and is NOT generated.

On the commit named above: 9863f0d8 was T132's task branch, squash-merged into the chain
tip immediately afterwards and therefore no longer reachable by that hash. Its tree is
identical to the merged commit apart from this file and T132's own tasks.md note. Recorded
as it ran rather than rewritten to name the merge, because the merge is not what was tested.

Re-recorded 2026-08-16 by T132, from a clean tree, at the chain tip. The previous record
was taken at commit 39ca0065 — eight commits and five production source changes behind
HEAD by the time T132 opened, and further behind by the time it ran. This is the second
time the artifact went stale in two days: T096 filed it, T132 found it recurring one day
later. Both times every gate had in fact been run; what drifted was the record.

Which is why nobody types this file any more. `just release-stack 003-real-files-and-tabs`
runs the same nine steps in the same order and writes everything above this line. It does
not retry a red gate — an automatic retry inside the thing that records gate results is how
a real regression gets averaged away — and it does not write this section.

WHAT THE NUMBERS WERE

  just check      80 suites, 637 tests, all passed. Lint 0 errors and the 2 pre-existing
                  react-refresh warnings, unchanged.
  npm test        80 suites, 637 tests — the same set, run again standalone.
  just e2e-test   268 passed, 0 failed, 5.1 minutes.
                  [parity states] 40/40 additional states covered by a named assertion.
                  [parity accounting] 150/150 attempted, 150 passed, 0 unaccounted.
  just verify     M1-M6 PASS against the recorded baseline, no new findings.
  just build      exit 0, build/bin/GoMarkEdit.app self-signed.

No flake this time. The 2026-08-15 record carried one — `T026 retains at least 20 measured
resize and divider samples`, failing on 0.75 of resize samples under 100 ms against a 0.95
threshold — and a related one recurred earlier in this session's T158 run, where
`FT-VS-10 dispatches the File accelerators the menu advertises` timed out on a tab count
while a Go race suite ran concurrently, then passed alone in 934 ms and passed 268/268 on
the full re-run. Both are wall-clock assertions on a loaded host, and both will fire again.
They are named here so the next red one is recognised rather than investigated from scratch.

WHAT THIS STACK DOES NOT COVER

Unchanged and still owed by a person: `just verify`'s M7 (new code is tested), M8 (no
placeholders), M11 (descriptive docs current) and M13 (scope declared).

Three interactive host walkthroughs are open and no gate here touches any of them, because
every suite above runs against the dev bridge mock or a Go test double:

  T166  Copy path and Reveal in file manager on the real binary. Both were dead in every
        shipped binary until T161 wired the host ports at the composition root.
  T181  SC-FT-007's autosave latency spot-checked on the `just build` artifact. T121 proved
        the measured write path is the same object code the release binary links, and could
        not drive the artifact itself — it has no write-commit observer and no input driver.
  T186  SC-FT-011's uncovered clauses: preserved permissions after a real save, and the
        mapped webview chrome compared against the same-browser result.

`just package` is deliberately not run — it exits non-zero on purpose until Phase 08.
The trailing `just gen-check` after `just build` is cleanup for the wailsjs mode bits, not
a gate result, and is deliberately not listed above.

The stack rewrites `evidence/ft-vs-08/offline-and-controls/five-minute-request-denial.{json,log}`
as a side effect of the e2e run. The script names them on exit; they were restored, not
committed, so this record's diff is the record.
