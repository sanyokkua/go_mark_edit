# Feature 003 release gate stack
commit: 7eff3a33
host:   Darwin arm64, go go1.26.6, node v24.18.0
start:  2026-08-14T13:57:49Z

just gen-check                  exit=0
just archtest                   exit=0
just check                      exit=0
just build                      exit=0
go test -race (4 pkgs)          exit=0
npm test --runInBand            exit=0
just verify 003-real-files-and-tabsexit=0

just e2e-test                   NOT GREEN — see e2e-status.md
end:    2026-08-14T14:05Z (approx)

Notes
-----
`just verify 003-real-files-and-tabs` reports M1-M6 PASS against the recorded
baseline and no new findings. It also states what it does NOT cover: M7 new code
is tested, M8 no placeholders, M11 docs current, M13 scope declared, and the
walkthrough — all owed by a person.

`just e2e-test` is the one gate not green. It is NOT broken and it is NOT
unreliable: it collects and runs (191 tests in 10 files, after a17c49f9 gave
each e2e file one runner). Every behavioural assertion in it passes. It is red
on exactly two things, both recorded with their evidence:

  1. 25 committed screenshot baselines written 2026-08-06, before this feature's
     specification existed, with 48 shell-surface commits since. Re-approval is
     an owner action; FR-FT-055 forbids accepting a baseline to make a gate pass.
     See ft-vs-08/regressions/baseline-provenance.md.
  2. The unrestricted 546-case matrix (T035), whose failures are measured in
     ft-vs-08/phase-18/t035-run-2026-08-14.md.

A gate that runs and reports a real, explained failure is doing its job. This is
recorded as red rather than waived.
