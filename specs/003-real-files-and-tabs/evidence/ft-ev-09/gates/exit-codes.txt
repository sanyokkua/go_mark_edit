# Feature 003 release gate stack
commit: 39ca0065
host:   Darwin arm64, go go1.26.6, node v24.18.0
start:  2026-08-15T09:48:04Z

just gen-check                         exit=0
just archtest                          exit=0
just check                             exit=0
go test -race ./internal/... .         exit=0
npm test --runInBand                   exit=0
go test -race -tags native_evidence ./cmd/...  exit=0
just e2e-test                          exit=1
just e2e-test                          exit=0
just verify 003-real-files-and-tabs    exit=0
just build                             exit=0

end:    2026-08-15T10:02:55Z

Notes
-----
Re-recorded 2026-08-15 by T038. The previous record was taken at commit 3befc43e,
before the T104 autosave wiring changed production Go and before this session's
T107 fix, so it did not cover the code it was filed against.

`just e2e-test` is 258 passed, up from the 256 baseline by exactly the two
FT-VS-09 cases T107 added. They belong to the `chromium` project and run once;
`repeatEach: 3` applies only to `parity`.

One flake, recorded rather than hidden. The first run of this stack was
`just e2e-test exit=1` — 257 passed, with
`T026 retains at least 20 measured resize and divider samples` failing on a
timing ratio: 0.75 of resize samples under 100 ms where the assertion requires
0.95 (`e2e/helpers/shell-observation.ts:44`). Re-run alone it passed in 16.4 s,
and the full suite then passed 258/258. The machine was concurrently running a
Wails build and the packaged application during the first attempt.

That is a load-sensitive assertion, not a regression: the same suite had already
passed 258/258 earlier in the session with every change of this session in place,
and nothing touched here affects window-resize timing. It is recorded because a
gate that goes red must be reported, and because the assertion is worth watching
— it measures wall-clock responsiveness and will fail again on a loaded host.

`go test -race -tags native_evidence ./cmd/...` is new to this stack. The
untagged gate matches no packages under ./cmd, so the native-evidence harness —
including T105's new explicit-save scenario — is invisible to `just check`. It is
run and retained here so the timing harness is covered by something.

Gate order follows AGENTS.md: gen-check first, build last. `just build` rewrites
frontend/wailsjs/runtime/ at mode 644, so gen-check is run once more afterwards
to regenerate them at 755 and leave the tree clean. That trailing run is cleanup,
not a gate result, and is not listed above.

`just package` is deliberately not run — it exits non-zero on purpose until
Phase 08.

`just verify 003-real-files-and-tabs` reports M1-M6 PASS against the recorded
baseline. What it does not cover is unchanged and still owed by a person: M7, M8,
M11, M13. The walkthrough it names is no longer owed — it was done on 2026-08-15
and is retained in ../host-walkthrough-2026-08-15.md with host screenshots.
