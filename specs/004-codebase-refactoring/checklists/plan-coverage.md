# Plan-to-Spec Coverage Checklist: Codebase Refactoring

**Purpose**: Validate that the planning artifacts (`plan.md`, `research.md`, `data-model.md`, `quickstart.md`, `contracts/`) fully and faithfully reflect `spec.md` — every requirement, scenario, clarification and boundary has a stated home in the plan, and nothing in the plan weakens, strengthens or invents behaviour.
**Created**: 2026-09-09 · **Reviewed**: 2026-09-09 (all 38 items satisfied after the plan-side edits recorded per item)
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md)

**Note**: This custom checklist is generated by the `$speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Requirement Coverage (spec → plan)

- [x] CHK001 Does every functional requirement FR-001 to FR-084 appear in the plan's requirement-coverage table with an owning task group and named evidence, with no requirement covered only by a generic group label? [Completeness, Spec §Requirements, Plan §Requirement coverage]
  - Finding (2026-09-09): All 84 FRs present; the eight-requirement G8 row was split (FR-072, -073, -074, -075/078/081, -076, -077 each with producible evidence) and FR-071 gained the missing-tool check (quickstart 5).
- [x] CHK002 Does every success criterion SC-001 to SC-015 name evidence in the plan that is specific enough to be produced (a test, a case, a sentence, a run id) rather than "reviewed at close"? [Measurability, Spec §Success Criteria, Plan §Requirement coverage]
  - Finding (2026-09-09): SC-004 now names lint L22–L24 plus the "Forbidden assertion kinds" deletions; SC-013 now maps the eleven capabilities to E2E cases and walkthrough steps (Capability inventory table); SC-014 enumerates its six outcomes.
- [x] CHK003 Are the nineteen Story 1 defects each mapped to exactly one regression level both trees share, as FR-031 requires, with the archive mechanism named per row? [Completeness, Spec §US1, §FR-031, Plan §Story 1 defects]
  - Finding (2026-09-09): 19 rows, one archive mechanism each: rows 7 and 16 reduced to the E2E level; row 19 fixed as a throwaway frontend test in the worktree (planning decision 6).
- [x] CHK004 Is the white-box exception list limited to behaviours the plan shows to be unreachable through public interfaces, each with its stated reason, so that FR-023's "listed with its reason" is satisfied? [Clarity, Spec §FR-023, Plan §White-box exception list]
  - Finding (2026-09-09): W1–W3 each state why the behaviour is unreachable publicly; lint L23 lists exactly those three files; no export shim.
- [x] CHK005 Does the plan name the fault lever for every one of the eight FR-026 cases and, for each lever-less case, the exact frontend-plus-Go split the spec allows? [Completeness, Spec §FR-026, Plan §Real-backend E2E]
  - Finding (2026-09-09): Eight cases with a lever or "none" each; cases 4 and 5 name the frontend-integration + Go-integration split for their lever-less half.
- [x] CHK006 Does every Go, Jest, `node --test` and Playwright suite marked delete or rewrite have its proved behaviours and their new cover named, with "gap" recorded where nothing covers a behaviour, as FR-033 requires? [Completeness, Spec §FR-033, Plan §Per-suite coverage]
  - Finding (2026-09-09): Verified against the tree at 2b889cb: 66 Go, 72 + 9 Jest, 2 node --test, 11 Playwright files, every one in a per-suite row; gap line present.
- [x] CHK007 Is every entry of `docs/delivery/plan/KNOWN_ISSUES.md` given one of the three FR-084 dispositions (fixed, moved to the map as an open decision, removed as stale)? [Completeness, Spec §FR-084, Plan §Known-issues reconciliation]
  - Finding (2026-09-09): All 17 entries have a row; entry 5 changed from a conditional to "stale → removed" (en.json holds status.lineEnding.crlf/mixed; the backend emits only utf-8).
- [x] CHK008 Are the walkthrough steps in the plan sufficient to cover every outcome FR-027 lists as observable from outside the process (launch, native Open, Save As, close and quit dialogs, files on disk, links opened in the system browser)? [Coverage, Spec §FR-027, Plan §Walkthrough step list]
  - Finding (2026-09-09): Steps 1, 3, 5, 7, 8, 13, 14 cover launch, native Open, Save As, close and quit dialogs, files on disk and system-browser links.
- [x] CHK009 Are the shared components of FR-034 to FR-043 each given inputs and a consumer inventory in the contracts, and does each inventory match the consumer list the spec states (for example the nine Popup consumers of FR-034, the toolbar-only ToolButton of FR-038)? [Consistency, Spec §FR-034–FR-043, Contracts §shared-components]
  - Finding (2026-09-09): Inventories match the spec (Popup 9, ToolButton toolbar only, ModalShell 7, Button 3, Bar 3); Icon gained its consumer inventory and Segmented's consumers were made concrete.

## Acceptance-Scenario Evidence

- [x] CHK010 Does every acceptance scenario of Stories 1–7 resolve to a named test, E2E case or walkthrough step in the plan, rather than to a task group alone? [Traceability, Spec §User Scenarios, Plan §Requirement coverage]
  - Finding (2026-09-09): Added "Acceptance-scenario evidence, Stories 2–7" (35 rows) to plan.md; Story 1 already had its table.
- [x] CHK011 Are the timing outcomes of SC-014 (notice within 1 s of the 10 s bound, exactly-once Retry, 60 s eviction, user-paced silence, Cancel-then-result, startup-step timeout before ready) each assigned an evidence level the plan can actually run (fake timers, E2E case 6 variants)? [Measurability, Spec §SC-014, Plan §Requirement coverage]
  - Finding (2026-09-09): SC-014 row now lists the six outcomes with their evidence, including a Go integration test re-sending Save with the same request id.
- [x] CHK012 Are the six theme/mode combinations and the "pointer and keyboard" opening modes of FR-009 / SC-007 both stated in the E2E case the plan names, with the computed-style property to compare? [Clarity, Spec §FR-009, §SC-007, Contracts §e2e-harness case 8]
  - Finding (2026-09-09): E2E case 8 states the six combos (auto via emulateMedia), pointer and keyboard opening, and getComputedStyle(popup).boxShadow versus --win-shadow.
- [x] CHK013 Is the "fails on the archived tree" evidence of SC-001 defined with the fields the spec requires (date, commit, host, observed failure) and a stated disposition for a defect not reproducible on the archive host? [Completeness, Spec §SC-001, §Edge Cases, Plan §Story 1 defects]
  - Finding (2026-09-09): Fields (date, commit bc185c9, host, observed failure) stated; the non-reproducible disposition now adds the by-hand verification on the host it needs.

## Consistency between plan artifacts and the spec

- [x] CHK014 Do plan, research and contracts use the spec's glossary terms (bridge, adapter, projection, stage, entry-point script, notification surface, architecture map, archived tree) without introducing synonyms? [Consistency, Spec §Glossary]
  - Finding (2026-09-09): Glossary terms used consistently; the two harnesses are qualified as real-backend and pixel-parity.
- [x] CHK015 Do the five script names, their sub-commands (`build dev`, `build setup`) and the `verify` forms in the contract match FR-060 exactly, with no additional flag or recipe? [Consistency, Spec §FR-060, Contracts §scripts-cli]
  - Finding (2026-09-09): Names, sub-commands and verify forms match FR-060; the two extra flags (setup --with-browser, --help) are now planning decision 5.
- [x] CHK016 Do the hook commands and the two workflow invocations in the contracts match FR-061 word for word (pre-commit `format --check`, pre-push plain `verify`, push `verify --skip e2e`, release plain `verify`)? [Consistency, Spec §FR-061, Contracts §scripts-cli, §ci-workflows]
  - Finding (2026-09-09): Hook and workflow commands match FR-061 word for word; the push.yml heading no longer claims a pull-request trigger.
- [x] CHK017 Does the format ignore list in the plan and contracts contain only the paths FR-063 names, and is any addition (the `justfile`) recorded as an explicit planning decision rather than left implicit? [Consistency, Spec §FR-063, Plan §Decisions taken during planning]
  - Finding (2026-09-09): Ignore list = the FR-063 paths, untracked node_modules, and the justfile recorded as decision 4.
- [x] CHK018 Do the shutdown states, timeouts and confirmation wording in `contracts/shutdown-protocol.md` and `data-model.md` agree with each other and with FR-016 to FR-018 ("Quit and discard" / "Cancel", 10 s, exit at once before ready)? [Consistency, Spec §FR-016–FR-018, Contracts §shutdown-protocol, Data model §CloseRequest]
  - Finding (2026-09-09): States, 10 s and wording agree across contract, data model and R10; the Confirming→cancel branch now covers the never-ready case (confirmation offered again).
- [x] CHK019 Do the pacing lists (user-paced commands) in `bridge-requests.md` and `data-model.md` name the same four commands as FR-019? [Consistency, Spec §FR-019, Contracts §bridge-requests]
  - Finding (2026-09-09): The same four commands in FR-019, bridge-requests.md, data-model.md and R9.
- [x] CHK020 Do the retention values of the outcome cache (60 seconds, 256 entries, oldest first) appear identically in the contract, the data model and the research decision? [Consistency, Spec §FR-021]
  - Finding (2026-09-09): 60 s / 256 / oldest-first now restated in data-model.md, identical to the contract and R9.
- [x] CHK021 Do the link and image policies in `research.md` R17, `data-model.md` and E2E cases 1 and 7 list the same accepted extensions, the same 20 MB bound and the same refusal outcome as FR-014 and FR-049? [Consistency, Spec §FR-014, §FR-049]
  - Finding (2026-09-09): R17 now lists the four extensions; E2E case 1 gained the untitled-document refusal and names the unit classifier test for javascript:/data:; 20 MB and the refusal outcome agree everywhere.
- [x] CHK022 Do the test-root paths in the plan (`tests/go/{unit,integration}`, `frontend/tests/{unit,integration,e2e}`) and the tier definitions in the contracts match FR-022's placement-by-dependency rule, including where the white-box in-package tests run? [Consistency, Spec §FR-022, Contracts §scripts-cli]
  - Finding (2026-09-09): Roots match FR-022; the three white-box files run in the integration tier because they use real files.
- [x] CHK023 Do the numbers the plan quotes from the tree (bound methods, test-file counts, struct fields, recover blocks) match the working tree, or are they marked as audit figures where they differ? [Accuracy, Plan §Technical Context]
  - Finding (2026-09-09): Verified on the tree: 66 Go test files, 72 + 9 Jest, 11 Playwright, 39 recover blocks, 39 fields / 9 maps, 35 bound methods; the 49 k test-line figure is now marked as the audit's.

## Clarification carry-through

- [x] CHK024 Is every answer in the spec's Clarifications section traceable to a plan or contract statement (for example: no adapter double below E2E, zero E2E retries, HOME/XDG redirect, Recents seeding, no native-dialog automation, baseline by instruction only)? [Traceability, Spec §Clarifications]
  - Finding (2026-09-09): Every listed answer traces to a plan or contract statement (R7, R8, harness, scripts-cli).
- [x] CHK025 Are the three owner decisions taken during planning (formatting scope, icon script, held-lock lever) and the review's fourth (justfile) each stated with the spec requirement they interpret, so none reads as a silent requirement change? [Clarity, Plan §Decisions taken during planning, Spec §FR-063, §FR-082, §FR-026]
  - Finding (2026-09-09): Decision 4 now cites FR-063; decisions 5–7 added, each naming the requirement it interprets.
- [x] CHK026 Where research recorded a fact that changed a clarification's assumption (inert FIFO lever, self-healing garbage database, settings reads never failing), does the plan state the replacement lever or split for each affected case? [Consistency, Research §Facts that changed a clarification's assumption, Spec §FR-026]
  - Finding (2026-09-09): Inert FIFO → held lock (case 6); self-healing database → directory lever (cases 2, 3); settings reads → split (case 4).

## Scope boundaries and non-goals

- [x] CHK027 Does the plan add no capability beyond the local image rendering of FR-049, and is every "new" component, binding or route (Popup, `OpenPreviewLink`, the asset route, request identity) traceable to a requirement rather than to a design preference? [Scope, Spec §Assumptions, §SC-013]
  - Finding (2026-09-09): Popup→FR-034, OpenPreviewLink→FR-014/R17, asset route→FR-049, request identity→FR-019, constructor options→FR-054, e2e-seed→FR-025, pendingClose→FR-016; no other addition.
- [x] CHK028 Are the Spec Kit-owned paths (`.specify/**`, `speckit-*` skills, extensions) excluded from every plan action, including formatting, deletion and instruction rewriting? [Boundary, Spec §FR-077, §Assumptions]
  - Finding (2026-09-09): Spec Kit paths excluded from formatting, deletion and rewriting; the extension output docs/superpowers/ and docs/reference/ now have a disposition (decision 7, Retained line).
- [x] CHK029 Does the plan preserve the "what already works and must stay" list (backend authority, adapter-only bindings, handler shape, tokenised colours, CGO-free backend and SQLite, atomic writes, sanitisation, bounded inputs) without a design that would alter any of them? [Negative scope, Spec §Background]
  - Finding (2026-09-09): Each "must stay" item has a preserving element: L8, L4, L18, L26, R11, the database integration coverage, sanitisation unchanged, bounds.
- [x] CHK030 Are the retained artefacts of FR-082 (product icons under `build/`, the tab-icon SVG, the audit report, the 2026-07-28 archive) explicitly excluded from the plan's removal list? [Boundary, Spec §FR-082, Plan §Structure Decision]
  - Finding (2026-09-09): A Retained (FR-082) line now names the icons, the tab-icon SVG, the audit report, the 2026-07-28 archive, docs/reference and docs/superpowers.
- [x] CHK031 Is the deletion order the spec's edge case demands (mock and parity stack only after the real-backend E2E passes; `docs/delivery` only after the map carries its decisions) stated as binding in the task-group order? [Sequencing, Spec §Edge Cases, §FR-032, Plan §G7, §G8]
  - Finding (2026-09-09): G7 deletes only after scripts/test e2e is green; G8 archives docs/delivery only after the map carries its decisions; order stated as binding.

## Constitution alignment

- [x] CHK032 Does the plan's constitution check argue each principle from a concrete plan element (a lint rule, an owner, a test root) rather than restating the principle? [Clarity, Constitution §I–VIII, Plan §Constitution Check]
  - Finding (2026-09-09): Each principle row argues from lint rules, task groups and named tests.
- [x] CHK033 Is the requirement that a timeout alone never authorises discarding data (Constitution §V, FR-018) reflected in every path of the shutdown state machine? [Consistency, Contracts §shutdown-protocol]
  - Finding (2026-09-09): Every path to Exiting with unsaved changes passes through Confirming; deadline branches require nothing unsaved.
- [x] CHK034 Are the constitution's test rules (no source-text, CSS-text, DTO-order, struct-shape or document assertions; no test-only branch in production) each owned by a named lint rule or a named deletion in the plan? [Completeness, Constitution §VII, Spec §FR-029, §FR-054, Contracts §lint-rules]
  - Finding (2026-09-09): lint-rules.md now maps each forbidden assertion kind and the test-only branch to its named deletions and recurrence guard.

## Ambiguities and open points

- [x] CHK035 Is the record for the feature-close walkthrough unambiguous, given that the constitution asks for a "recorded walkthrough" and the spec's session-thirteen answer says "no written record" while FR-027 and the plan require one sentence in the plan? [Conflict, Spec §Clarifications, §FR-027, Constitution §VII]
  - Finding (2026-09-09): spec.md no longer contains the "no written record" answer; the final-review answer (one sentence in the plan) is in force, matches the constitution's recorded walkthrough, and the close-out slot follows it.
- [x] CHK036 Is the duration of the auto-dismissing refused-link notice either specified or explicitly delegated to the existing warning-toast rule, so implementation does not invent a value? [Ambiguity, Spec §FR-014, Contracts §shared-components §Notifications]
  - Finding (2026-09-09): The refused-link notice is now delegated to the notification surface's warning duration (8 s in Toast.tsx today); no new value.
- [x] CHK037 Are the spike items in research (no relaunch after quit, asset route reachable in browser mode) each paired with the consequence for the plan if the spike fails, so a failed spike is a recorded decision rather than an improvised change? [Assumption, Research §R8]
  - Finding (2026-09-09): Both R8 spikes now carry a failure consequence in research.md and the harness contract.
- [x] CHK038 Is the "second frontend" effect of `wails dev` (the native window on the same backend) addressed in every E2E case that counts calls, events or readiness, not only in the harness preamble? [Assumption, Contracts §e2e-harness]
  - Finding (2026-09-09): Cases 2, 3, 5 and 6 each state what the second frontend does; the preamble records that a repeated WindowReady is idempotent.

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `$speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `$speckit-specify` and `$speckit-clarify`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
